# 04-model-comparison
An automated n8n workflow that analyzes security alert text using multiple AI models (sentiment analysis, zero-shot classification, named entity recognition, and LLM reasoning) and stores the results in Airtable for comparison.
